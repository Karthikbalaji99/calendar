{
  "version": 2,
  "buildCommand": "npm run build",
  "routes": [
    { "src": "^/api($|/.*)", "dest": "/api/index" },
    { "handle": "filesystem" },
    { "src": ".*", "dest": "/index.html" }
  ]
}
